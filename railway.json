{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build && npm run db:push"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node dist/index.js --port=${PORT:-3000}",
    "restartPolicyMaxRetries": 5
  }
}
